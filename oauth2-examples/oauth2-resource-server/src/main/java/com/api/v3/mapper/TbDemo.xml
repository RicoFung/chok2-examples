<?xml version="1.0" encoding="UTF-8"?>  
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">   

<mapper namespace="com.api.v3.dao.tbdemo.TbDemoDao">
	<!-- 开启基于redis的二级缓存 -->
	<!-- 
    <cache type="chok.redis.mybatis.RedisCache">
    	<property name="eviction" value="LRU" />
        <property name="flushInterval" value="6000000" />
        <property name="size" value="1024" />
        <property name="readOnly" value="false" />
    </cache>
     -->
     
	<sql id="columns">
		T.TC_ROWID, 
		T.TC_CODE, 
		T.TC_NAME 
	</sql>	
	
	<sql id="dynamicWhere">
		<where>
    		<if test="@chok.devwork.Ognl@isNotEmpty(tcRowid)"> AND T.TC_ROWID = #{tcRowid} </if>
	       	<if test="@chok.devwork.Ognl@isNotEmpty(tcCode)"> AND T.TC_CODE LIKE '%'||#{tcCode}||'%'</if>
	       	<if test="@chok.devwork.Ognl@isNotEmpty(tcName)"> AND T.TC_NAME LIKE '%'||#{tcName}||'%'</if>
			<if test="@chok.devwork.Ognl@isNotEmpty(tcRowidArray)"> 
				AND T.TC_ROWID IN
				<foreach collection="tcRowidArray" item="tcRowid" index="index" open="(" close=")" separator=",">#{tcRowid}</foreach>
			</if>
		</where>
	</sql>	
	
	<!-- getOne -->
	<resultMap type="com.api.v3.entity.TbDemoGetOneResult" id="getOneResult" >
        <result property="tcRowid" column="TC_ROWID"/>
        <result property="tcCode" column="TC_CODE"/>
        <result property="tcName" column="TC_NAME"/>
	</resultMap>
	<select id="getOne" resultMap="getOneResult">
		SELECT <include refid="columns"/> 
		  FROM TB_DEMO T 
		 WHERE T.TC_ROWID = #{tcRowid}  	  
	</select>
	
	<!-- getList -->
	<resultMap type="com.api.v3.entity.TbDemoGetListResult" id="getListResult" extends="getOneResult">
	</resultMap>
	<select id="getList" parameterType="com.api.v3.dao.tbdemo.param.TbDemoGetListParamPO" resultType="list" resultMap="getListResult">
		SELECT <include refid="columns"/> 
		  FROM TB_DEMO T
		<include refid="dynamicWhere" />
	</select>
</mapper>  
